# import math
# radius = float(input("Enter the radius of the circle :"))
# if radius >= 0:
#     area = math.pi*(radius**2)
#     print("the radius is non negative .")
#     print(f"the area of hte circle is:{area}")
# else:
#     print("invalid input:  Radius cannot be negative.")

# import math
# radius = float(input("Enter the radius of the circle"))
# if radius >= 0:
#     area = math.pi*(radius**2)
#     print("the radius is non negative")
#     print(f"the area is of the circle :{area}")
# else:
#     print("invalid input:radius cannot be negative")
import math
radius = float(input("Enter the radius of the circle:")) 
if radius >= 0:
    area = math.pi*(radius**2)
    print("the radius is non negative")
    print(f"enter the radius of the circle:{area}")
else:
    print("invalid input: radius cannot be negative") 